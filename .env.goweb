# Go54 / cPanel Production Environment Configuration
# Upload this file to your server root and rename it to .env

# MySQL Database URL
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE_NAME
# Note: Password special characters are URL encoded:
# + -> %2B, ; -> %3B, , -> %2C, = -> %3D, $ -> %24
DATABASE_URL=mysql://miemploy_hris:qEbnUR%24%3B%2B%2C%3DDk~n2@127.0.0.1:3306/miemploy_miemploya

# Port (Passenger usually overrides this, but good to have)
PORT=5000

# Environment
NODE_ENV=production

# Session Secret (Change this to a random string)
SESSION_SECRET=go54_production_secret_change_me
